import { Badge } from "react-bootstrap";

const LactoseFreeBadge = () => {
  return (
    <Badge pill bg="primary">
      <span>lactose-free</span>
    </Badge>
  );
};

export default LactoseFreeBadge;
